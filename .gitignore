# Environment variables
.env

# Python caches
__pycache__/
*.py[cod]
*.class
*.log
.venv/
venv/

# Crawler and Playwright caches
.crawl4ai/
.playwright/

# Data and snapshots (keep structure, ignore json snapshots if needed, or ignore all data/snapshots)
# data/snapshots/*.json

# Archives
archives/

# Runtime outputs (tracked demo artifacts remain under data/ and reports/)
runtime/

# Cloudflare Wrangler cache
.wrangler/

# 静态站点构建产物：由 tools/build_static_site.py 生成，CI 在部署前重新构建。
# 过去它被提交进仓库，导致 dist/screenshots 与 data/screenshots 各存一份
# 相同的 13MB 截图，且产物会随构建平台漂移。
dist/

# Temporary scratch scripts
scratch/
